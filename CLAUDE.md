# Development Guide

**uv** is used for package management.

```sh
# install package
uv add <package_name>

# activate virtual environment
source .venv/bin/activate

# run script
uv run <script>
```

